# Used by docker compose and is the main place to define the versions
HUGO_VERSION=0.156.0
GO_VERSION=1.25
# Reserved for Phase 2 — not yet consumed by any build. Node will build the
# slide decks (Vite/reveal.js); Python runs the keepup digest.
NODE_VERSION=22
PYTHON_VERSION=3.14