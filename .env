# Used by docker compose and is the main place to define the versions
HUGO_VERSION=0.156.0
GO_VERSION=1.25
# Node builds the slide decks (Vite/reveal.js); Python runs the keepup digest.
# Pinned here so local, CI, and the Cloudflare build all agree on one version.
NODE_VERSION=22
PYTHON_VERSION=3.14